services:
  docs:
    image: fuse-docs:0.4.0
    build:
      context: ..
      dockerfile: docs/Dockerfile
    ports:
      - "4080:4080"
    environment:
      PORT: "4080"
      FUSE_HOST: "0.0.0.0"
      FUSE_STATIC_DIR: "/app/docs"
