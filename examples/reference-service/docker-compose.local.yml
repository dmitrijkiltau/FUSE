services:
  migrate:
    build:
      context: ../..
      dockerfile: examples/reference-service/Dockerfile.local
    entrypoint: ["fuse", "migrate", "."]
    environment:
      FUSE_DB_URL: "sqlite:///app/data/reference-service.db"
    volumes:
      - db-data:/app/data
    restart: "no"

  app:
    build:
      context: ../..
      dockerfile: examples/reference-service/Dockerfile.local
    depends_on:
      migrate:
        condition: service_completed_successfully
    ports:
      - "3000:3000"
    environment:
      APP_PORT: 3000
      FUSE_HOST: "0.0.0.0"
      FUSE_DB_URL: "sqlite:///app/data/reference-service.db"
    volumes:
      - db-data:/app/data

volumes:
  db-data:
