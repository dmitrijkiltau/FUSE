fn state_block(message: String, compact: Bool, error: Bool) -> Html:
  var class_name = "state"
  if compact:
    class_name = class_name + " state--compact"
  if error:
    class_name = class_name + " state--error"
  return p({"class": class_name}):
    html.text(message)
